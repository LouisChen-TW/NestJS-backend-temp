version: "3"
services:
  redis:
    image: redis
    ports:
      - 6379:6379
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
    ports:
     - 3306:3306
   